# API Reference

<!-- Start at Task 1 with the register endpoint; add every endpoint in
     the task that builds it. This doubles as your Postman companion. -->

## Conventions

<!-- Base URL, auth header format, and the error response shape.
     Document the three-way error distinction here:
     - 401: missing or invalid/expired credentials
     - 403: valid credentials, resource isn't yours
     - 404: resource doesn't exist at all
     Endpoint-by-endpoint tables below then only note exceptions. -->

## Auth

<!-- Per endpoint: method + path, auth required (yes/no/role), request
     fields, success response, failure cases. Prose or small tables —
     no literal payloads needed unless you find them useful. -->

## Properties

## Rooms

## Bookings

## Search
